export enum UserRole {
  admin = 'admin',
  superadmin = 'superadmin',
  operator = 'operator',
  teacher = 'teacher',
  student = 'student',
}

export enum UserStatus {
  pending = 'pending',
  active = 'active',
  inactive = 'inactive',
}
